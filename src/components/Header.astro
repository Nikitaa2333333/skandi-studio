---
// Липкая шапка без фона — секции проезжают под ней.
// Пункты меню подставить свои.
const nav = [
  { href: '#', label: 'Раздел' },
  { href: '#', label: 'Раздел' },
  { href: '#', label: 'Раздел' },
  { href: '#', label: 'Раздел' },
];
---

<header class="header">
  <div class="container header__row">
    <a href={import.meta.env.BASE_URL} class="header__logo">Логотип</a>
    <nav class="header__nav">
      {nav.map((item) => <a href={item.href}>{item.label}</a>)}
    </nav>
    <a href="#form" class="header__cta">Кнопка</a>
  </div>
</header>

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 100;
  }
  .header__row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    padding-block: 16px;
  }
  .header__logo {
    font-weight: 500;
    font-size: var(--fs-body);
  }
  .header__nav {
    display: flex;
    gap: clamp(16px, 2.5vw, 36px);
    font-size: var(--fs-small);
  }
  .header__cta {
    font-size: var(--fs-small);
    color: var(--c-green);
    font-weight: 450;
  }
  @media (max-width: 760px) {
    .header__nav { display: none; }
  }
</style>