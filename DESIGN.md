# Дизайн-система — сайт Оксаны Чащиной

Референс: [farmminerals.com/products/croptab](https://www.farmminerals.com/products/croptab)
(разобран по живому CSS/JS сайта + спек [designmd.me/s/farmminerals-q244t](https://designmd.me/s/farmminerals-q244t)).
Берём у референса: воздух, швейцарскую типографику, full-bleed сетку с узкими полями,
тонкие линии, техничные подписи, характер анимаций. Меняем: базу с бежевой на
бело-серую, зелёного меньше — он акцент, а не фон; капс запрещён; скругления убраны в ноль.

---

## 1. Настроение

Природный, воздушный, контрастный. Швейцарская типографика: крупные тонкие
заголовки, много белого, иерархия размером и воздухом, а не жирностью и цветом.
Фото — полноширинные или половина сетки, всегда «дышат». Линии тонкие, 1px,
полупрозрачные — только там, где они структура (спек-листы, шапка, футер).

## 2. Цвета

Референс живёт на бежевом `#f4ede6` с шестью зелёными. У нас база — белый и
серый ближе к белому, зелёные — те же оливково-авокадовые, но дозированно.

| Токен | Значение | Роль |
|---|---|---|
| `--c-bg` | `#ffffff` | база страницы |
| `--c-surface` | `#f4f4f1` | серый ближе к белому: чередование секций (аналог реф. `#f8f3f0`, но без бежевого подтона) |
| `--c-ink` | `#111311` | текст — почти чёрный, капля зелени |
| `--c-green` | `#5c6b3c` | авокадовый акцент: теги, ссылки, детали (реф. `#636f3d`) |
| `--c-green-deep` | `#404f1d` | тёмные контрастные секции (реф. hero: `#404f1d`, pinned: `#596a33`) |
| `--c-green-pale` | `#aab98f` | пастельный шалфей: детали на тёмном, `::selection`, `<mark>` (реф. `#a5b589`) |
| `--c-line` | `#dddddd` | структурные линии на светлом |
| `--c-white` | `#ffffff` | текст на тёмном — чистый белый, без приглушения |

### Розовый — второй акцент, «внешняя» линия

Принцип двух цветовых линий повторяет концепцию сайта «Две половины одной работы»:

- **Зелёный = внутреннее / медицина**: организм, причины старения, превентивка,
  тёмные секции, служебный UI (шапка, ссылки, фокус формы, футер).
- **Розовый = внешнее / эстетика**: кожа, процедуры, уход. Применяется только
  в секциях процедурного трека — целиком (плашка + бирки), а не точечно.

| Токен | Значение | Роль |
|---|---|---|
| `--c-rose` | `#c08379` | бирки, номера, детали в процедурных секциях |
| `--c-rose-pale` | `#e9cfc8` | пастельная подложка при необходимости |
| `--c-rose-surface` | `#f7edea` | пудровая плашка процедурных секций |

Механика: `.section--rose` (фон) + `.accent-rose` (бирки) вешаются на секцию
вместе. В сплите «Снаружи/Изнутри» половина «Снаружи» — розовая, «Изнутри» —
зелёная: это якорь принципа. Розовый и тёмно-зелёный в одной секции не смешиваем;
`<mark>` остаётся шалфейным везде.

Правила из референса, которые сохраняем:

- на тёмной секции линии — белые/зелёные с прозрачностью (`rgba(255,255,255,.25)`,
  у рефа hairline всегда 1px на 30–50% прозрачности);
- зелёные рядом не смешивать: одна секция — один зелёный;
- hover — сдвигом opacity (`.7`), не сменой цвета;
- яркие насыщенные цвета запрещены; красный только для ошибок формы.

## 3. Типографика

Шрифт — **Onest Variable** (гротеск с кириллицей, ближайший свободный аналог
Helvetica; референс сидит на Aeonik 400/500). Один шрифт на всё, иерархия —
размером и лёгкими шагами веса.

| Роль | Токен | Размер | Вес | LH | Трекинг |
|---|---|---|---|---|---|
| Display (имя на первом экране) | `--fs-display` | clamp 44→86px | 300 | 1.02 | −0.015em |
| H1 страницы | `--fs-h1` | clamp 36→61px | 330 | 1.02 | −0.01em |
| H2 секции | `--fs-h2` | clamp 28→40px | 330 | 1.12 | −0.01em |
| H3 / лид | `--fs-h3` | clamp 20→24px | 380–420 | 1.4 | 0 |
| Набор | `--fs-body` | clamp 16→17.5px | 400 | 1.5 | 0 |
| Мелкая подпись | `--fs-small` | 14px | 400 | 1.4 | 0 |
| Технический лейбл `.tag` | `--fs-label` | 12px | 400 | 1.2 | +0.04em |

Приёмы референса, которые переносим:

- **отрицательный трекинг только на крупных заголовках** (−0.01…−0.03em у рефа),
  у набора и подписей трекинг нулевой;
- **line-height заголовков ниже 110%** (у рефа 90–93%) — плотные, скульптурные строки;
- тонкость вместо жирности: реф добавляет вес text-stroke'ом, мы — вариативным весом 300–420.

Наши отличия (жёсткие):

- **капса нет нигде** — у рефа все лейблы капсом, у нас те же техничные подписи
  строчными, зелёным цветом (`.tag`); в CSS стоит предохранитель `text-transform: none`;
- жирность внутри наборного текста запрещена, выделение — только `<mark>`
  (подложка `--c-green-pale`, прямоугольная, одна строка);
- мера строки токенами в `ch` на самом текстовом элементе:
  `--measure-text: 72ch`, `--measure-lead: 60ch`, `--measure-head: 26ch`,
  `--measure-head-lg: 18ch`, `--measure-prose: 70ch`.

## 4. Сетка и отступы

Референс — **full-bleed**: контейнер не ограничен, поля всего `1.88em ≈ 1.8vw`
(23px @1280), ширину держат сами модули. Мы делаем то же ощущение «прижатости
к экрану», но с потолком на сверхшироких:

| Токен | Значение | Роль |
|---|---|---|
| `--page-max` | `1360px` | потолок контента (реф: без потолка; спек: 1388px) |
| `--gutter` | `clamp(14px, 1.8vw, 26px)` | узкое боковое поле — как у рефа |
| `--section-gap` | `clamp(48px, 7vw, 96px)` | вертикальный ритм секции (низ+верх соседних = разрыв ×2) |
| `--section-gap-tight` | `clamp(28px, 4vw, 56px)` | поджатый вариант |
| `--block-gap` | `clamp(20px, 3vw, 40px)` | заголовок секции → контент |

Модификаторы: `.section--tight`, `.section--flush-top`, `.section--flush-bottom`,
`.section--surface` (серая плашка-секция), `.theme-dark` (тёмно-зелёная).
Подробности пульта — в `SPACING.md`.

- **Скругления: 0 везде** (у рефа мелкие .3–.6em — сознательно убираем, прямые углы).
- Чередование ритма страницы как у рефа: фото-hero → текст → серая секция →
  тёмно-зелёная контрастная → фото 50/50 со спек-листом → слайдер/карточки → CTA-фото → FAQ.
- Фото: либо во всю ширину экрана (`.bleed`), либо половина grid `1fr 1fr`
  с высотой во вьюпорт (реф: `height:100vh; max-height:80em`).
- Перечисления — без буллитов и линий, ритм отступом ~13px.
- Линии только структурные: спек-листы, граница шапки, футер, строки таблиц.

## 5. Фирменные модули (из референса)

- **Спек-лист** («1 tablet = 2.5 acres»): grid-строка `1fr + узкая колонка`,
  слева крупное значение (~40px, зелёный), справа мелкая расшифровка, снизу
  hairline 1px на 50% прозрачности. Появление построчное. У нас — для цифр
  практики («22 года», «60 минут консультация», ступени работы).
- **Карточка-паспорт** (реф: квадрат N/P/K с микро-лейблами по углам и крупной
  буквой в центре) — для методов: крупный номер/буква + лейблы глубины/эффекта.
- **Кнопка**: у рефа капс между двух точек в тонкой рамке; у нас — строчными
  в рамке/заливке 1px, прямые углы, hover opacity.
- **Крестик-плюс аккордеона** из двух 1px-дивов (не иконка) — для FAQ и
  свёрнутого блока «Образование».
- **Полоса-примечание** на тёмно-зелёном (реф `.drop-note`, высота ~90px) — для
  обязательной строки «Имеются противопоказания…».

## 6. Анимации

Референс (то самое «кайф»-ощущение) собран на: `gsap 3.15 + ScrollTrigger +
SplitText`, `lenis 1.1.13` (плавный скролл, lerp 0.1), Splide для слайдера.
Что переносим и как:

1. **Посимвольное появление заголовков** — фирменный приём рефа: SplitText
   режет на chars, символы проявляются `autoAlpha 0→1`, duration 0.4s,
   `stagger 0.02` **в случайном порядке**, старт при `top 90%` вьюпорта.
   У нас — то же на ванильном JS (`data-split` + IntersectionObserver +
   случайные `transition-delay`), без 90КБ GSAP — сайт должен оставаться мгновенным.
2. **Ревил абзацев и карточек** — мягкий fade/rise (24px) с каскадом ~0.08s
   внутри группы: `.reveal`, `.reveal-group` (готово в `global.css`).
3. **Построчный ревил спек-листов** (реф: атрибут `NumbersText`) — строка +
   её hairline появляются последовательно.
4. **Параллакс фото** (реф: `image="parallax"`) — лёгкий сдвиг фона при скролле,
   добавим точечно на полноширинные фото.
5. **Pinned-сцены** (реф: секция 300–550vh со sticky-обёрткой 100vh, внутри
   карточки разлетаются) — кандидат для «Как строится работа» (3 ступени),
   подключать позже и осторожно.
6. **Плавный скролл Lenis** — опционально вторым шагом (9КБ), если захочется
   именно «тягучести» рефа.
7. Hover всегда `opacity .7 / 0.2s ease`. `prefers-reduced-motion` — всё
   отключается (готово).

## 7. Do / Don't

**Do:** белый фон по умолчанию; серые секции для смены ритма; одна тёмно-зелёная
контрастная секция на страницу-две; техничные зелёные подписи-теги строчными;
тонкие крупные заголовки; hairline 1px на прозрачности; фото с воздухом.

**Don't:** капс; жирный/курсив в наборе; приглушённый и полупрозрачный текст
(на тёмном — только чистый белый); скругления; буллиты и разделительные линии
в списках; текст на всю ширину без меры; смешивать два зелёных в одной секции;
тени (макс. `0 2px 8px rgba(0,0,0,.1)` на редких плавающих элементах).
